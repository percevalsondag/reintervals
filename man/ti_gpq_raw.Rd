% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpq.R
\name{ti_gpq_raw}
\alias{ti_gpq_raw}
\title{GPQ tolerance interval from raw data (direct engine, advanced)}
\usage{
ti_gpq_raw(
  y,
  time,
  batch,
  t0,
  P = 0.95,
  alpha = NULL,
  level = NULL,
  sides = c("two.sided", "lower", "upper"),
  c_resid = 1,
  control = lmm_interval_control()
)
}
\arguments{
\item{y}{Numeric response.}

\item{time}{Numeric time/age covariate (same length as \code{y}).}

\item{batch}{Batch factor (same length as \code{y}).}

\item{t0}{Time point(s) at which to evaluate the interval.}

\item{P}{Content proportion (default 0.95; pharma stability typically 0.99).}

\item{alpha, level}{Confidence as a mutually-exclusive pair (give at most one);
\code{level = 1 - alpha}, default \code{alpha = 0.05}.}

\item{sides}{\code{"two.sided"} (default), \code{"lower"}, or \code{"upper"}.}

\item{c_resid}{Residual multiplier \code{c} in \code{N(theta, tau2 + c*s2e)}: \code{1}
(default) a future observation, \code{0} the batch-mean.}

\item{control}{\code{\link[=lmm_interval_control]{lmm_interval_control()}} tuning (the GPQ \code{seed} and \code{M}).}
}
\value{
A data frame, one row per \code{t0}: \code{method = "gpq"}, \code{time}, \code{estimate},
\code{lower}, \code{upper}, \code{design = "random_slope_correlated"}, plus audit fields
(\code{tau2_med}, \code{s2e_med}, \code{frac_boundary}, \code{K}, \code{df_resid}, \code{phi1}).
}
\description{
The Oliva-Aviles & Hauser (2025) generalized-pivotal-
quantity tolerance interval for the random-intercept + random-slope stability
model, from raw \verb{(y, time, batch)} vectors without a fitted \code{lmer} model. The
fit-based dispatcher \code{\link[=ti_lmm]{ti_lmm()}} routes random-slope designs here automatically
(singular fits, and the batch-mean target \code{over = "group_mean"}); use
\code{ti_gpq_raw()} directly when you have data but no model object.
}
\examples{
fm <- lme4::lmer(Reaction ~ Days + (1 + Days | Subject), data = lme4::sleepstudy)
d <- lme4::sleepstudy
ti_gpq_raw(d$Reaction, d$Days, d$Subject, t0 = 5,
           control = lmm_interval_control(seed = 1, M = 2000))
}
\references{
Oliva-Aviles C, Hauser S (2025). \emph{Technometrics} 67(2):193-202. \doi{10.1080/00401706.2024.2407324}
}
\seealso{
\code{\link[=ti_lmm]{ti_lmm()}} (the dispatcher), \code{\link[=ti_anova_raw]{ti_anova_raw()}} (the ANOVA raw engine),
\link{reintervals-models}.
}
