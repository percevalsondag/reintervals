% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/re-interval.R
\name{new_re_interval}
\alias{new_re_interval}
\alias{re_interval}
\title{Interval result object}
\usage{
new_re_interval(
  estimate,
  lower,
  upper,
  type,
  conf = NA_real_,
  level = NA_real_,
  content = NA_real_,
  df = NA_real_,
  sides = "two",
  target = NA_character_,
  design = "",
  call = NULL
)
}
\arguments{
\item{estimate}{Numeric scalar point estimate (\code{lb-hat}).}

\item{lower, upper}{Numeric scalar interval bounds.}

\item{type}{Character: \code{"CI"}, \code{"PI"}, or \code{"TI"}.}

\item{conf}{Confidence level (CI: \code{1 - alpha}; TI: \code{gamma}). \code{NA} if not used.}

\item{level}{Prediction level (PI: \code{1 - psi}). \code{NA} if not used.}

\item{content}{Content fraction (TI: \code{beta}). \code{NA} if not used.}

\item{df}{Degrees of freedom used for the bound (\code{NA} for the normal-based TI).}

\item{sides}{Character: \code{"two"} (two-sided intervals only).}

\item{target}{Character target distribution (\code{"observable"} / \code{"true_value"}),
or \code{NA} for the CI (which concerns the mean, not a future value).}

\item{design}{Character naming the random structure, or \code{""}.}

\item{call}{The originating call, or \code{NULL}.}
}
\value{
An object of class \code{re_interval}.
}
\description{
The shallow S3 value returned by the EMS method-pure constructors. It records
the point estimate, the two-sided bounds, and the metadata identifying which
interval was computed. It is internal plumbing: the user-facing verbs return
the unified \code{lmm_interval} data frame, not this object.
}
\details{
Provides \code{print} and \code{as.data.frame} methods.
}
\keyword{internal}
