% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dispatch.R
\name{ti_lmm}
\alias{ti_lmm}
\title{Tolerance interval for a linear mixed model}
\usage{
ti_lmm(
  model,
  newdata = NULL,
  P = 0.95,
  over = c("observation", "group_mean"),
  alpha = NULL,
  level = NULL,
  sides = c("two.sided", "lower", "upper"),
  method = "auto",
  control = lmm_interval_control()
)
}
\arguments{
\item{model}{A fitted \code{lmerMod}.}

\item{newdata}{Row(s) giving the fixed-effect combination(s); the time column
supplies the evaluation point(s). \code{NULL} uses the first model-frame row.}

\item{P}{Content proportion (the population fraction covered). Default 0.95
(pharma stability work typically passes \code{P = 0.99}).}

\item{over}{What the interval covers: \code{"observation"} (default; a proportion
\code{P} of future observations --- the ordinary tolerance interval) or
\code{"group_mean"} (a proportion \code{P} of new group means --- the batch-mean
tolerance interval).}

\item{alpha, level}{The confidence, as a mutually-exclusive pair: give at most
one. \code{level} is \code{1 - alpha}; default \code{alpha = 0.05}.}

\item{sides}{\code{"two.sided"} (default), \code{"lower"}, or \code{"upper"}.}

\item{method}{\code{"auto"} (default) chooses the algorithm by structure; one of
\code{"ems-mls"}, \code{"reml-mls"}, \code{"anova-mls"}, \code{"gpq"} forces it and errors if
incompatible with the model.}

\item{control}{\code{\link[=lmm_interval_control]{lmm_interval_control()}} tuning (GPQ seed / M).}
}
\value{
An \code{lmm_interval} object (a data frame), one row per evaluation point,
with columns, in order:
\itemize{
\item the eval-point column(s), named by the actual model predictor(s)
(e.g. the time covariate for a slope model, or each \code{newdata} column);
\item \code{type} (\code{"ci"}, \code{"pi"}, \code{"ti"}, or \code{"new_group_mean"}), \code{estimate},
\code{lower}, \code{upper};
\item \code{level} (the confidence, \code{1 - alpha}); \code{P} (content proportion ---
the value for \code{ti}, \code{NA} for the coverage intervals); \code{sides};
\item \code{method} (the engine: \code{"ems-mls"}, \code{"reml-mls"}, \code{"anova-mls"}, or
\code{"gpq"}); \code{design} (the route-independent model class, see
\link{reintervals-models});
\item the per-row flags \code{singular}, \code{boundary}, \code{vg_floored}.
}
\code{attr(., "diagnostics")} is a list of engine-specific extras (degrees of
freedom \code{df}, standard error \code{se}, and any GPQ / ANOVA intermediates);
\code{attr(., "grouping")} is the random-intercept topology (\code{"oneway"},
\code{"nested"}, \code{"crossed"}, or \code{NA} for slope models).
}
\description{
One of the four interval verbs, and the dispatched tolerance interval. It
classifies the fitted model and routes by random/fixed structure, the
tolerance target (\code{over}), and the singular flag to
the engine that is exact/validated for that case --- all transparently:
}
\details{
\itemize{
\item \strong{Random slope.} \code{over = "observation"} uses the REML closed-form MLS
interval when the fit is non-singular and the GPQ interval when singular;
\code{over = "group_mean"} always uses GPQ.
\item \strong{Random intercept + fixed slope.} Clean balanced -> EMS engine; single-
observation / staggered / unbalanced -> the Montes ANOVA/MLS closed form.
\item \strong{Random intercept, no fixed slope} -> EMS engine.
}

For slope models the between-group variance \code{V_G(t0)} is quadratic in the
time covariate, so evaluating at a \code{t0} outside the observed time range is
an extrapolation of that quadratic and should be done knowingly (the interval
is computed without a guard or warning). The same applies to the other slope
verbs (\code{\link[=ci_lmm]{ci_lmm()}}, \code{\link[=pi_lmm]{pi_lmm()}}, \code{\link[=new_group_mean_lmm]{new_group_mean_lmm()}}).
}
\section{Availability}{

Tolerance intervals require a closed-form variance decomposition. They are
available for one-way, nested, and crossed-with-interaction random-intercept
designs, the fixed-slope stability design, and random-slope models. For a
design without such a decomposition (two crossed factors with no interaction
term, or four or more grouping factors), ti_lmm() signals an error; ci_lmm(),
pi_lmm(), and new_group_mean_lmm() remain available for those models.
}

\examples{
# random intercepts, no slope -> EMS engine
fi <- lme4::lmer(Reaction ~ Days + (1 | Subject), data = lme4::sleepstudy)
ti_lmm(fi, newdata = data.frame(Days = 0))
# random slope, future-observation -> REML closed form
fs <- lme4::lmer(Reaction ~ Days + (1 + Days | Subject), data = lme4::sleepstudy)
ti_lmm(fs, newdata = data.frame(Days = 5))
# random slope, batch-mean -> GPQ
ti_lmm(fs, newdata = data.frame(Days = 5), over = "group_mean",
       control = lmm_interval_control(seed = 1))
}
\seealso{
\code{\link[=ci_lmm]{ci_lmm()}}, \code{\link[=pi_lmm]{pi_lmm()}}, \code{\link[=new_group_mean_lmm]{new_group_mean_lmm()}}, \code{\link[=lmm_predict]{lmm_predict()}},
\link{reintervals-models}.
}
