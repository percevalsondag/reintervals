% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intervals-api.R
\name{new_group_mean_lmm}
\alias{new_group_mean_lmm}
\title{New-group-mean (CInew / new-batch-mean) interval}
\usage{
new_group_mean_lmm(
  model,
  newdata = NULL,
  alpha = NULL,
  level = NULL,
  sides = c("two.sided", "lower", "upper"),
  method = "auto",
  control = lmm_interval_control()
)
}
\arguments{
\item{model}{A fitted \code{lmerMod} (from \code{\link[lme4:lmer]{lme4::lmer()}}).}

\item{newdata}{Row(s) giving the fixed-effect combination(s) to evaluate at.
\code{NULL} uses the first model-frame row.}

\item{alpha, level}{The interval confidence, as a mutually-exclusive pair: give
at most one. \code{level} is \code{1 - alpha}; the default is \code{alpha = 0.05} (95\%).}

\item{sides}{\code{"two.sided"} (default), \code{"lower"}, or \code{"upper"}.}

\item{method}{\code{"auto"} (default) chooses the algorithm by structure; one of
\code{"ems-mls"}, \code{"reml-mls"}, \code{"anova-mls"}, \code{"gpq"} forces that engine and
errors if it is incompatible with the model.}

\item{control}{An \code{\link[=lmm_interval_control]{lmm_interval_control()}} object (the GPQ \code{seed} and \code{M}).}
}
\value{
An \code{lmm_interval} object (a data frame), one row per evaluation point,
with columns, in order:
\itemize{
\item the eval-point column(s), named by the actual model predictor(s)
(e.g. the time covariate for a slope model, or each \code{newdata} column);
\item \code{type} (\code{"ci"}, \code{"pi"}, \code{"ti"}, or \code{"new_group_mean"}), \code{estimate},
\code{lower}, \code{upper};
\item \code{level} (the confidence, \code{1 - alpha}); \code{P} (content proportion ---
the value for \code{ti}, \code{NA} for the coverage intervals); \code{sides};
\item \code{method} (the engine: \code{"ems-mls"}, \code{"reml-mls"}, \code{"anova-mls"}, or
\code{"gpq"}); \code{design} (the route-independent model class, see
\link{reintervals-models});
\item the per-row flags \code{singular}, \code{boundary}, \code{vg_floored}.
}
\code{attr(., "diagnostics")} is a list of engine-specific extras (degrees of
freedom \code{df}, standard error \code{se}, and any GPQ / ANOVA intermediates);
\code{attr(., "grouping")} is the random-intercept topology (\code{"oneway"},
\code{"nested"}, \code{"crossed"}, or \code{NA} for slope models).
}
\description{
One of the four interval verbs: a prediction interval for the mean of one new,
as-yet-unobserved group/batch: the CInew / new-batch-mean interval. It
is the prediction interval with the residual component removed,
\eqn{\mu(t_0) \pm t_{\nu_G}\sqrt{V_F + V_G}}; coverage is \code{level}/\code{alpha}, so
it carries no content \code{P} (unlike \code{\link[=ti_lmm]{ti_lmm()}}). Dispatch and arguments are
as for \code{\link[=ci_lmm]{ci_lmm()}}.
}
\examples{
fm <- lme4::lmer(Reaction ~ Days + (1 + Days | Subject),
                 data = lme4::sleepstudy)
new_group_mean_lmm(fm, newdata = data.frame(Days = 5))
}
\seealso{
\code{\link[=ci_lmm]{ci_lmm()}}, \code{\link[=pi_lmm]{pi_lmm()}}, \code{\link[=ti_lmm]{ti_lmm()}}, \code{\link[=lmm_predict]{lmm_predict()}},
\link{reintervals-models}.
}
